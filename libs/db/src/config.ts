import { postgresConfig } from '@newsnow/env-config'

// const { POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USERNAME, POSTGRES_PASSWORD, POSTGRES_DATABASE } =
//   postgresConfig()

// export const host = POSTGRES_HOST
// export const port = POSTGRES_PORT
// export const username = POSTGRES_USERNAME
// export const password = POSTGRES_PASSWORD
// export const database = POSTGRES_DATABASE
// export const databaseUrl = `postgres://${username}:${password}@${host}:${port}/${database}`
export const databaseUrl = 'dev.db'
